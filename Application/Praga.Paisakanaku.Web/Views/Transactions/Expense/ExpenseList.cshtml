@model Response<List<ExpenseInfo>>

@{
    ViewData["Title"] = "Expenses";
}
<style>

  /* body {
    margin: 40px 10px;
    padding: 0;
    
  } */

  #calendar {
    font-family: Arial, Helvetica Neue, Helvetica, sans-serif;
    max-width: 1100px; 
    margin: 0 auto;
    font-size: 14px;
  }

    .fc-event-title-container{
        background: darkred;
        font-size: 15px;
        text-align: right;
    }

    th.fc-col-header-cell.fc-day {
        background: darkslateblue;
        padding: 12px;
    }

    a.fc-col-header-cell-cushion {
        color:white;
        text-decoration: none;
    }

    a.fc-daygrid-day-number{
        color: black;
        text-decoration: none;
        cursor:pointer;
    }

</style>

<script src="~/lib/full-calendar/index.global.min.js" asp-append-version="true"></script>

<div id='calendar'></div>

<script src="~/js/transactions/expense/expense.js" asp-append-version="true"></script>

@* <div class="action-body-container">
    <div class="main-grid-container">
        <div class="table-responsive">
            <table class="table table-bordered  align-middle">
                <thead>
                    <tr>
                        <th scope="col">Date</th>
                        <th scope="col">Expense Amount</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.IsSuccess && Model.Data != null && Model.Data.Any())
                    {
                        @foreach (var expense in Model.Data)
                        {
                            <tr>
                                <th scope="row">
                                    <span>@expense.Date</span>
                                </th>
                                <th scope="row">
                                    <span>@expense.Amount</span>
                                </th>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div> *@